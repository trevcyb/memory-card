import React from "react";
import Card from "./Card";

const cardDisplay = (props) => {
  const { gameLogic, score, highScore } = props;

  let images = [];

  const [cards, shuffleCards] = useState(images);
};
